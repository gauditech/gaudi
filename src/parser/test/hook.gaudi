model User {
  field fieldOne {
    type text
    validate {
      custom hook {
        source "customValidateHook"
      }
    }
  }
  field fieldTwo {
    type text
    validate {
      custom hook {
        inline `
          return first.toLowerCase() === second.toLowerCase();
        `
      }
    }
  }
}
