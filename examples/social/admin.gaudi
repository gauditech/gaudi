config admin {
    autoadmin true
    exclude GroupMembership GroupApplication
    baseurl /admin
}

admin User {
    computed num_of_posts count posts
    
    transform {
        omit auth
    }
    pick posts
    pick posts_commented_on {
        pick 
    }
}